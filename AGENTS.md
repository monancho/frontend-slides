# AGENTS.md

이 repository는 `frontend-slides` 스킬을 사용해 HTML 기반 발표자료를 만들기 위한 작업 공간이다.

발표자료 작업을 요청받으면 먼저 `.agents/skills/frontend-slides/SKILL.md`를 읽는다.

최종 산출물은 기본적으로 `output/` 폴더에 저장한다.

슬라이드 본문은 기본적으로 한국어로 작성한다.

실제 발표자료 생성 요청이 있기 전까지는 `output/presentation.html`을 생성하지 않는다.
